<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/sofor_ana"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SoforAnaActivity">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:layout_width="match_parent"
            android:id="@+id/sofor_toolbar"
            android:layout_height="70dp"
            app:titleTextColor="@color/yesil"
            android:layout_marginTop="30dp"
            android:elevation="4dp"
            android:background="@drawable/servisresimsiz"/>
        
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/sofor_fragment_container"/>
    </LinearLayout>

    <com.google.android.material.navigation.NavigationView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/sofor_nav"
        android:layout_gravity="start"
        app:headerLayout="@layout/sofor_header"
        app:menu="@menu/sofor_menu"
        app:itemTextColor="@color/yesil"/>

</androidx.drawerlayout.widget.DrawerLayout>